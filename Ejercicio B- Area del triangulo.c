#include <stdio.h>
#include <math.h>
int main(int argc, char *argv[]) {
	int base,altura,resultado;
	scanf("%d" "%d",&altura,&base);
	resultado=fabs(base*altura)/2;
	printf("%i",resultado);
	return 0;
}
